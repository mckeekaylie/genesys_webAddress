# genesys_webAddress

1. Install Node
2. Install Angular CLI
3. Install dependencies npm i
4. Clone and run the server https://github.com/mckeekaylie/genesys_webAddress_server
5. After changing directories to where this client is housed, run ng s -o to run the application. Once running, the application can be accessed at localhost:4200.
